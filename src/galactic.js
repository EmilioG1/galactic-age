export class AgeCalc {
  constructor(age, lifeExpectancy) {
    this.age = age;
    this.lifeExpectancy = lifeExpectancy;
    let planets = [('mercury', 0.24), ('venus', 0.62), ('mars', 1.88), ('jupiter', 11.86)];
  }
  whatAge() {
    
  }
}
